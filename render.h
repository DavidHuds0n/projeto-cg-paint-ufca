#ifndef RENDER_H
#define RENDER_H

// Função principal de renderização, chamada pelo displayCallback em main.c
// Delega o desenho dos objetos para o módulo objects.
void renderAllObjects();

#endif // RENDER_H
